我在制作BOS 1.0时，从BOS最基础的版本到发布我的BOS 1.0，至今已过去4个月之久，我在2025年12月开始编写BOS
就算是1.0版本，也可能有一些地方不好，标准化程度不高，但是我希望你们能理解，目前，BOS可能总是还会有一些奇奇怪怪的BUG，请见谅
另外，我是一个中国六年级小学生，我靠着兴趣走到现在
我的代码中可能没有多少注释（因为TurboWarp的注释功能有一些难用）
你可以在Bilibili联系我：XaoDingx，或者使用邮箱：qpwq1@outlook.com，但是邮箱我不常用，最好用Bilibili

关于文件系统：
文件系统的单文件上限是55KB，这不是随便设置的，这是TURBOWARP列表的硬性限制，文件系统不像内存那样，每两个Byte一个地址，文件系统是由20个列表项，
每一项50KB左右的1MB DISK

关于内存管理：
内存管理可以说的就多了，目前，操作系统管理内存是需要一个抽象层的，但是BOS直接使用物理地址，而且物理地址相当于了数组下标，并没有0xffffff，但是
这在我看来，不是缺点，因为我在我的BOS里从来没有想过安全性
BOS内存管理最大的寻址空间是2^16次方，你可能会问，这到底是驱动还是操作系统管理内存，这个问题问的匪巢，我的答案是：我在做全栈。2^16次方，按照我
的当前一个地址2Byte来算，应该是128KB的内存，但是我为什么选择64KB呢，是因为，我认为，TURBOWARP的列表可能撑不住128KB，稳定性肯定不够，我也想过
这个问题很久，但是最后还是选择了64KB。
还有，操作系统的内存用久了，都会有一个困境：你有很大的内存空间，但是他们不是连续的。为了解决这个问题，我思考了很久，但是我没有做，但是核心
原理就是首先把当前内存里的所有数据分页存储到文件系统里，然后清空内存，再重新写回去，但是这样的方法看起来可行，但是还是需要制定标准才可能运行
实行。
内存管理的地址，比如0x2，是以十进制存储的（也就是列表下标），但是实际上存储需要存16个二进制数（2字节）

另外我还要说，BOS由于历史遗留问题，导致0是TRUE，1是FALSE，我也不太敢修，就这样保持着吧。
                                                                        2026年3月18日 19：23 BOS作者
